package com.eleks.socialnetworkanalyser.entities

case class PostStats(postId : Int,
                     likeCount: Int,
                     dislikeCount : Int,
                     repostCount : Int
                    )
